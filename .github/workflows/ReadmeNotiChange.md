from pathlib import Path

content = """# GitHub Repository Change Notification

This project includes a GitHub Actions workflow that automatically sends email notifications whenever important activities occur in the repository.

The workflow is designed to help team members monitor repository changes without having to check GitHub manually.

## Workflow Name

```text
Notify all repository changes
